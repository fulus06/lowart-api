services:
  api-server:
    build: .
    ports:
      - "8080:8080"
    environment:
      - DATABASE_URL=sqlite:/app/data/lowart.db?mode=rwc
      - MASTER_KEY=your_32_byte_master_key_here_plz
      - LOG_LEVEL=info
    volumes:
      - ./data:/app/data
    restart: always
